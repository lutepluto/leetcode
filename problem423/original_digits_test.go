package problem423

import (
	"fmt"
	"testing"
)

func TestOriginalDigits(t *testing.T) {
	fmt.Printf("%s is %s\n", "owoztneoer", originalDigits("owoztneoer"))
	fmt.Printf("%s is %s\n", "fviefuro", originalDigits("fviefuro"))
}
